apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  labels:
    app.kubernetes.io/part-of: campusbite
    app.kubernetes.io/component: auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/component: auth
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: campusbite
        app.kubernetes.io/component: auth
    spec:
      containers:
        - name: auth
          image: campusbite-auth
          imagePullPolicy: Always
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: campusbite-config
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: campusbite-secrets
                  key: jwt-secret
            - name: MONGO_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: campusbite-secrets
                  key: mongo-root-username
            - name: MONGO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: campusbite-secrets
                  key: mongo-root-password
            - name: MONGO_URI
              value: mongodb://$(MONGO_ROOT_USERNAME):$(MONGO_ROOT_PASSWORD)@mongo-0.mongo:27017/campusbite_auth?authSource=admin
          startupProbe:
            httpGet:
              path: /health
              port: 8004
            periodSeconds: 5
            failureThreshold: 18
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            periodSeconds: 8
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            periodSeconds: 20
          resources:
            requests:
              cpu: 25m
              memory: 96Mi
            limits:
              memory: 256Mi
